<?xml version="1.0" encoding="utf-8"?>
<layer-list xmlns:android="http://schemas.android.com/apk/res/android">

    <item>
        <shape android:shape="oval">
            <solid android:color="@color/black"/>
            <stroke android:color="@color/white" android:width="@dimen/medium"/>

            <!-- Set the same value for both width and height to get a circular shape -->
        </shape>

    </item>
    <item android:left="@dimen/big" android:top="@dimen/big" android:right="@dimen/big" android:bottom="@dimen/big">
        <shape android:shape="oval">
            <solid android:color="@color/red"/>

            <!-- Set the same value for both width and height to get a circular shape -->
            <size android:width="250dp" android:height="250dp"/>
        </shape>
    </item>
    <item android:drawable="@drawable/ic_photo_camera" android:left="@dimen/llarge" android:top="@dimen/llarge" android:right="@dimen/llarge" android:bottom="@dimen/llarge">
    </item>
</layer-list>